## SecretSentinel Shared Types (stub)

This directory will contain shared type definitions and protocol contracts used across the monorepo.

Planned contents:

- Shared data models for findings, secrets, projects, and environments.
- gRPC/Protobuf or JSON schema definitions (if adopted).
- TypeScript declaration files or Go/Python/TypeScript codegen outputs.

Implementation will be added alongside the higher-level services (Vault, rotation, API, dashboard).
